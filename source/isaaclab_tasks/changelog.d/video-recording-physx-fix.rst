Added
^^^^^

* Added a default :class:`~isaaclab.envs.common.ViewerCfg` to
  :class:`~isaaclab_tasks.direct.factory.factory_env_cfg.FactoryEnvCfg` so that
  Kit-camera video captures of Factory tasks frame the workspace by default
  instead of using the global viewer fallback.
